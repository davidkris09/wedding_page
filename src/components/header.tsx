import { HeaderTop, HeaderContainer, HeaderTitle } from '../styles/Header.styled'

const Header = () => {
    return(
        <HeaderTop>
            <HeaderContainer>
                <HeaderTitle></HeaderTitle>
            </HeaderContainer>
        </HeaderTop>
    )
}

export default Header;